ở folder chính dùng 2 lệnh "npm install" và "flutter pub get" để tải thư viện trước khi chạy bài 

dùng lệnh cd tới folder function nhập "npm install" vào terminal để tải các thư viện liên quan

dùng lệnh flutter emulators --launch Pixel_4_API_35 và flutter run để chạy app bằng giả lập điện thoại
